<div class="row">
  <div class="col-sm-2"></div>
  <div class="col-sm-8">
    <h1>Github user: <%= @user.name %></h1>

    <table class="table">
      <thead>
      <tr>
        <th>repository</th>
      </tr>
      </thead>

      <tbody>
      <% @user.github_repositories.each do |repo| %>
        <tr>
          <td>
            <%= link_to repo.github_path, github_user_repository_path(user_id: repo.github_user.name, id: repo.repository) %>
          </td>
        </tr>
      <% end %>
      </tbody>
    </table>
  </div>
  <div class="col-sm-2"></div>
</div>
